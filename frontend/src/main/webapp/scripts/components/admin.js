'use strict';

(function(module) {
  module.exports = function(React) {
    var Settings = React.createClass({
      render: function() {
        return (
          <div>
            <h2>Admin</h2>
          </div>
        );
      }
    });

    return Settings;
  }

}(module))
